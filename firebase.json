{
  "functions": {
    "predeploy": "yarn --prefix \"$RESOURCE_DIR\" run build",
    "source": "ts/functions"
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}
